# 포트폴리오 검색 페이지 UI/UX 디자인 계획

## 🎯 사용자 요구사항 (User Needs)

### 주요 시나리오
1. **검색 시작**: 메인 페이지에서 검색어 입력 → 검색 결과 페이지로 이동
2. **결과 탐색**: 검색 결과 확인, 필터링, 정렬
3. **상세 확인**: 관심 있는 포트폴리오 클릭 → 상세 페이지 이동

### 사용자 페인 포인트
- 검색 결과가 너무 많아 원하는 것을 찾기 어려움
- 검색어를 수정하고 싶을 때 다시 메인으로 돌아가야 함
- 어떤 필터를 적용할 수 있는지 모름

## 📐 정보 아키텍처 (Information Architecture)

```
검색 페이지 (/search?q=검색어)
│
├── 1. 검색 헤더 영역
│   ├── 검색바 (재검색 가능)
│   ├── 검색어 표시 ("검색어"에 대한 검색 결과)
│   └── 결과 개수 (총 N개)
│
├── 2. 필터 & 정렬 영역
│   ├── 정렬 옵션 (최신순, 인기순, 조회순)
│   ├── 필터 버튼 (Sheet 모달)
│   └── 활성 필터 뱃지
│
├── 3. 검색 결과 영역
│   ├── 결과 그리드 (4열 그리드)
│   ├── Empty State (결과 없음)
│   └── 로딩 상태
│
└── 4. 페이지네이션
    └── 페이지 번호 네비게이션
```

## 🎨 디자인 시스템 적용

### 컬러 팔레트
- **Primary**: `--color-primary-800` (#004A9C) - 검색바, 버튼
- **Background**: `--color-bg-primary` (#FFFFFF) - 메인 배경
- **Text**: `--text-primary` - 제목, `--text-secondary` - 부가정보
- **Border**: `--border-default` - 카드 테두리

### 타이포그래피
- **검색 헤더**: `heading-2` (32px, bold) - "검색어"에 대한 검색 결과
- **결과 개수**: `body-large` (18px, regular) - 부가 정보
- **카드 제목**: `heading-6` (16px, semibold)
- **카드 설명**: `body-small` (14px, regular)

### 스페이싱
- **섹션 간격**: `--space-16` (64px)
- **컴포넌트 간격**: `--space-6` (24px)
- **카드 간격**: `--space-3` (12px) - 그리드 gap

### 컴포넌트
- **검색바**: 기존 SearchBar 컴포넌트 재사용 (일부 수정)
- **필터**: ExploreFilters 컴포넌트 재사용
- **카드**: PortfolioCard 컴포넌트 재사용 (home/portfolio-card.tsx 또는 새로 생성)
- **Empty State**: 커스텀 컴포넌트 필요

## 🎭 컴포넌트 구조

### 1. SearchHeader 컴포넌트
```typescript
<SearchHeader
  query="검색어"
  resultCount={24}
  onSearch={(newQuery) => {...}}
/>
```

**기능**:
- 검색어 표시
- 검색 결과 개수 표시
- 검색바 (재검색 가능)

**접근성**:
- `aria-label="검색 결과 헤더"`
- 검색 결과 개수는 `aria-live="polite"` 로 스크린 리더에 알림

### 2. SearchFilters 컴포넌트
기존 `ExploreFilters` 재사용 (동일한 UI/UX)

**기능**:
- 카테고리 필터 (웹 개발, 모바일 앱, UI/UX 디자인 등)
- 학과 필터
- 정렬 옵션 (최신순, 인기순, 조회순)
- 활성 필터 뱃지 표시

### 3. SearchResults 컴포넌트
```typescript
<SearchResults
  results={portfolios}
  isLoading={false}
  query="검색어"
/>
```

**상태별 UI**:
- **로딩 중**: Skeleton 그리드 (4개 skeleton 카드)
- **결과 있음**: PortfolioCard 그리드
- **결과 없음**: EmptyState 컴포넌트

### 4. EmptyState 컴포넌트
```typescript
<EmptyState
  query="검색어"
  onClearSearch={() => {...}}
/>
```

**내용**:
- 아이콘 (Search X)
- 메시지: "검색어"에 대한 검색 결과가 없습니다
- 제안:
  - 다른 검색어로 시도해보세요
  - 철자가 정확한지 확인해주세요
  - 더 일반적인 검색어를 사용해보세요
- CTA 버튼: "전체 포트폴리오 보기" → /explore

## 📱 반응형 디자인

### 데스크톱 (1440px+)
- 검색 결과 그리드: 4열
- 필터 Sheet: 오른쪽 사이드 패널

### 태블릿 (768px - 1023px)
- 검색 결과 그리드: 2-3열
- 필터 Sheet: 전체 너비 모달

### 모바일 (< 768px)
- 검색 결과 그리드: 1-2열
- 필터 Sheet: 하단 Sheet
- 검색바 높이 조정

## ♿ 접근성 고려사항

1. **키보드 네비게이션**
   - Tab으로 모든 인터랙티브 요소 접근 가능
   - 검색바 자동 포커스 (선택적)
   - 필터 Sheet 열릴 때 포커스 트랩

2. **스크린 리더**
   - 검색 결과 개수는 `aria-live="polite"`
   - 필터 적용 시 결과 변경 알림
   - Empty State에 적절한 aria-label

3. **색상 대비**
   - WCAG AA 준수 (최소 4.5:1 대비)
   - 아주대 블루 (#004A9C)는 흰색 텍스트와 충분한 대비

4. **포커스 인디케이터**
   - `--shadow-focus-primary` 사용
   - 키보드 포커스 명확하게 표시

## 🔄 사용자 플로우

```
[메인 페이지]
    ↓ 검색어 입력 & 엔터
[검색 페이지] /search?q=검색어
    ↓
    ├─ 결과 확인
    ├─ 필터 적용 → URL 업데이트 (쿼리 파라미터)
    ├─ 재검색 → URL 업데이트
    └─ 카드 클릭 → [포트폴리오 상세 페이지]
```

## 🎯 성능 고려사항

1. **이미지 최적화**
   - Next.js Image 컴포넌트 사용
   - Lazy loading
   - placeholder blur

2. **데이터 로딩**
   - Suspense 경계 사용
   - Skeleton UI로 로딩 상태 표시
   - 페이지네이션으로 초기 로드 최소화

3. **URL 상태 관리**
   - 검색어, 필터, 정렬을 URL 쿼리 파라미터로 관리
   - 브라우저 뒤로가기 지원
   - 공유 가능한 URL

## 🧪 테스트 시나리오

1. **검색 기능**
   - [ ] 검색어 입력 후 결과 표시
   - [ ] 빈 검색어 처리
   - [ ] 특수문자 포함 검색어 처리

2. **필터링**
   - [ ] 카테고리 필터 적용
   - [ ] 학과 필터 적용
   - [ ] 여러 필터 조합
   - [ ] 필터 초기화

3. **정렬**
   - [ ] 최신순 정렬
   - [ ] 인기순 정렬
   - [ ] 조회순 정렬

4. **엣지 케이스**
   - [ ] 검색 결과 0개
   - [ ] 검색 결과 매우 많음 (100+)
   - [ ] 네트워크 오류

5. **접근성**
   - [ ] 키보드만으로 모든 기능 사용
   - [ ] 스크린 리더 테스트
   - [ ] 색상 대비 검증

## 📋 구현 체크리스트

### Phase 1: 기본 구조
- [ ] `/search` 페이지 생성
- [ ] SearchHeader 컴포넌트 구현
- [ ] 기본 레이아웃 구성

### Phase 2: 검색 결과
- [ ] SearchResults 컴포넌트 구현
- [ ] PortfolioCard 재사용/생성
- [ ] 로딩 상태 (Skeleton)
- [ ] EmptyState 컴포넌트

### Phase 3: 필터링
- [ ] ExploreFilters 통합
- [ ] URL 쿼리 파라미터 연동
- [ ] 필터 상태 관리

### Phase 4: 페이지네이션
- [ ] Pagination 컴포넌트 (이미 있음)
- [ ] 페이지 상태 관리

### Phase 5: 마무리
- [ ] 반응형 테스트
- [ ] 접근성 검증
- [ ] 성능 최적화

## 🎨 디자인 모크업 (텍스트)

```
┌─────────────────────────────────────────────────────────────────┐
│  HEADER (app-header.tsx)                                        │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  [검색바 - 파란색 배경]  🔍 "포트폴리오를 검색해보세요"          │
│                                                                  │
│  "웹 개발"에 대한 검색 결과                          총 24개     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  [최신순] [인기순] [조회순]  |  [🎚️ 필터 (2)]                  │
│  [#웹 개발 ×] [#소프트웨어학과 ×]                               │
└─────────────────────────────────────────────────────────────────┘

┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 카드 1    │ │ 카드 2    │ │ 카드 3    │ │ 카드 4    │
│ [썸네일]  │ │ [썸네일]  │ │ [썸네일]  │ │ [썸네일]  │
│ 제목      │ │ 제목      │ │ 제목      │ │ 제목      │
│ 설명...   │ │ 설명...   │ │ 설명...   │ │ 설명...   │
│ 👤 작성자  │ │ 👤 작성자  │ │ 👤 작성자  │ │ 👤 작성자  │
└──────────┘ └──────────┘ └──────────┘ └──────────┘

┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 카드 5    │ │ 카드 6    │ │ 카드 7    │ │ 카드 8    │
└──────────┘ └──────────┘ └──────────┘ └──────────┘

              [ 1 ] [ 2 ] [ 3 ] ... [ 10 ]

┌─────────────────────────────────────────────────────────────────┐
│  FOOTER                                                         │
└─────────────────────────────────────────────────────────────────┘
```

---

**디자인 원칙 요약**:
1. ✅ User needs first - 검색어와 결과 명확히 표시
2. ✅ Progressive disclosure - 필터는 Sheet로 숨김
3. ✅ Consistent patterns - 기존 컴포넌트 최대한 재사용
4. ✅ Mobile-first - 반응형 그리드
5. ✅ Accessibility - 키보드, 스크린 리더, 포커스 관리
